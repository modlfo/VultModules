locale = "en-us"
baseurl = "https://modlfo.github.io/VultModules/"
title = "Vult Modules"
theme = "vultmodules"

relativeURLs = "true"

# No content uses tags or categories; without this Hugo emits empty
# /tags/ and /categories/ feeds.
[taxonomies]

[markup.goldmark.renderer]
unsafe= true
